import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.jcedeno.saccerqr',
  appName: 'scannerQR',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
